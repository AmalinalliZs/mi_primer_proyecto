# mi_primer_proyecto

Proyecto de prueba en Python para practicar:
- VS Code
- Entorno virtual (venv)
- Git y GitHub (ramas y pull requests)

## Cómo ejecutar
1. Crear entorno virtual:
   - Windows (PowerShell):
     .venv\Scripts\Activate.ps1

2. Instalar dependencias:
   pip install -r requirements.txt

3. Ejecutar:
   python main.py
